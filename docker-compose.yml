version: '2'

services:
  python-redmine-server:
    build: .
    ports:
      - 5001:5000
    volumes:
      - .:/src
    depends_on:
      - reverse-proxy
  reverse-proxy:
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf